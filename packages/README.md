# Packages

This repo is organized around two packages:

- `sandbox`: the Python SDK for creating and working with Modal Sandboxes.
- `sandbox_cli`: the command-line interface installed as `sandbox`.
