# Design

Design guidance for this repo is intentionally compact:

- Prefer boring, explicit APIs over broad abstractions.
- Preserve compatibility for existing imports when splitting modules.
- Keep CLI and SDK concepts aligned.
- Make resource creation visible in docs and command names.
- Use examples to demonstrate real workflows rather than marketing copy.

Detailed design notes live in `docs/design-docs/`.
