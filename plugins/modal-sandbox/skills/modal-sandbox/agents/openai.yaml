interface:
  display_name: "Modal Sandbox"
  short_description: "Run isolated, reproducible tasks in Modal Sandboxes"
  default_prompt: "Use $modal-sandbox when this task needs isolated, reproducible, or resource-controlled execution."
